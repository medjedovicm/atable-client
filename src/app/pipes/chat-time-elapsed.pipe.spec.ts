import { ChatTimeElapsedPipe } from './chat-time-elapsed.pipe';

describe('ChatTimeElapsedPipe', () => {
  it('create an instance', () => {
    const pipe = new ChatTimeElapsedPipe();
    expect(pipe).toBeTruthy();
  });
});
